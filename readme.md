# Other dependencies for a new system
brew install emacs --HEAD --use-git-head --with-cocoa \
    --with-gnutls --srgb --with-librsvg --with-ns

brew install aspell --with-lang-en

brew install zsh

brew install ctags
